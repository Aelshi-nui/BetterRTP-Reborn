# BetterRTPAddons

Optional addon module for BetterRTPx.

## Build

```bash
mvn clean package
```

This module depends on the core `BetterRTPx` artifact being available in the local Maven repository or reactor.
